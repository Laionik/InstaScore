@model Tuple<InstaScore.Models.LocalPasswordModel, InstaScore.Models.UserProfile>

@{
    ViewBag.Title = "Zarządzanie kontem";
    ViewBag.Nick = User.Identity.Name;
}

<hgroup class="title">
    <h1>@ViewBag.Title.</h1>
</hgroup>

<p class="message-success">@ViewBag.StatusMessage</p>

<p>
    Jesetś zalogowany jako <strong>@ViewBag.Nick</strong>.

@if (ViewBag.HasLocalPassword)
{
    @Html.Partial("_ChangePasswordPartial")
}
else
{
    @Html.Partial("_SetPasswordPartial")
}
@{
        @Html.Partial("_SetEmailPartial")
}


<section id="externalLogins">
    @Html.Action("RemoveExternalLogins")

    <h3>Dodaj zewnętrzne logowanie</h3>
    @Html.Action("ExternalLoginsList", new { ReturnUrl = ViewBag.ReturnUrl })
</section>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
